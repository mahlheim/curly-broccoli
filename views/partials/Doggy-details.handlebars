<section class="card">
  <a href="/dog/{{id}}">
    <header>{{name}}</header>

    <div class="card-body">
    <img src="/public/images/dog_images/{{filename}}" alt="{{name}}">
    <p>{{location}}</p>
    </div>
  </a>
</section>
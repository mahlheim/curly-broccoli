<section class="card">
  <a href="/dog/{{id}}">
    <header>{{name}}</header>

    <div class="card-body">
    <img src="/images/{{filename}}" alt="{{name}}">
    <p>{{location}}</p>
    </div>
  </a>
</section>